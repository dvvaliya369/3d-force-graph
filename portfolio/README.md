# Portfolio Site

A lightweight, static portfolio site (HTML/CSS/JS) living alongside this repo.

## Run locally

From the repo root:

```bash
npm run portfolio
```

Then open `http://localhost:5173`.

## Customize

- Update name/roles/links in `portfolio/index.html`.
- Edit colors and layout in `portfolio/styles.css`.
- Replace the project cards with real project links.

